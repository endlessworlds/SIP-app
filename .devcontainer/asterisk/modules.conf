[modules]
autoload=yes

; Use only PJSIP stack for this project's SIP/WebSocket flow.
; Prevent chan_sip from intercepting REGISTER requests on WebSocket.
noload => chan_sip.so

; Ensure SIP over WebSocket protocol handler is always present.
load => res_pjsip_transport_websocket.so
